// PostCSS config. I just copied this from the Tailwind docs. Works for now!
// TODO: Figure out if I need more plugins. Or not.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
    // old way: require('tailwindcss'),
  },
}
